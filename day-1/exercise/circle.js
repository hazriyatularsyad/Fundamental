// Write a code to find diameter, circumference and area of a circle.

const radius = 5
const diameter = radius * 2
const circumference = 2 * Math.PI * radius // 2 * phi * radius
const area = Math.PI * radius * radius // phi * radius * radius

console.log(Math.PI)
console.log({
    diameter,
    circumference,
    area
})